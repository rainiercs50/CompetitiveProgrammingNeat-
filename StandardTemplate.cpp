#include <bits/stdc++.h>
#define ar array
#define endl '\n'

using namespace std;
using ll = long long;

int main(){
	ios::sync_with_stdio(0); cin.tie(0); cout << fixed << setprecision(15);

	int T = 1; // cin >> T;
	auto solve = [&](int tc){
		
	};
	for(int tc = 0; tc < T; ++tc) solve(tc);
}
